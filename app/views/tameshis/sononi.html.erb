<h1>Tameshis#sononi</h1>
<p>Find me in app/views/tameshis/sononi.html.erb</p>

<p>試しその２です</p>

    <%= link_to '試し「その３」に行きます！！！', tameshis_sonosan_path, class: 'btn btn-block btn-black' %>

  <div class="container">
    <% @topics.each do |topic| %> <!--ここでトピックを繰り返しdoしている-->
      <div class="row">
        <div class="col-md-6 col-md-offset-3">
          <div class="topic-index-single">
            <h2 class="topic-author">
              <%= topic.user.name %>
            </h2>
            <%= image_tag topic.image.url %>
              <% if !topic.favorite_users.include?(current_user) %>
                <%= link_to favorites_path(topic_id: topic.id), method: :post do %> <!--routesのpostのとこに行くよ。そこには '/favorites', to: 'favorites#create'とかいてある-->
                  <%= image_tag 'icons/heart-black.png', class: 'topic-index-icon' %>
                <% end %>
              <% else %>
                <%= link_to favorites_path(topic_id: topic.id), method: :delete do %>
                  <%= image_tag 'icons/heart.png', class: 'topic-index-icon-red' %>
                <% end %>
              <% end %>
            <%= topic.favorite_users.count %>
            <%= image_tag 'icons/comment.png', class: 'topic-index-icon' %>
            <p class="topic-text">
              <%= topic.description %>
            </p>
          </div>
        </div>
      </div>
    <% end %>
  </div>